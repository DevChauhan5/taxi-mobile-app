import { Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const Chat = () => {
  return (
    <SafeAreaView>
        <Text>Chat Here</Text>
    </SafeAreaView>
  )
}

export default Chat